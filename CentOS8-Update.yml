---
  - name: Initializing OS && farmware packages update
    hosts: awxnode1
    gather_facts: no

    tasks:
      - name: Upgrade all packages
        ansible.builtin.dnf:
          name: "*"
          state: latest
